diff --git a/crates/synara-app/src/shell.rs b/crates/synara-app/src/shell.rs
index ac04cfd..1a39536 100644
--- a/crates/synara-app/src/shell.rs
+++ b/crates/synara-app/src/shell.rs
@@ -1129,7 +1129,7 @@ impl Shell {
             self.error = Some("Wait for saved attachments to load or finish saving before sending.".into());
             cx.notify(); return;
         }
-        if self.hub_navigation_blocked(cx) { return; }
+        if self.hub_send_blocked(cx) { return; }
         let Some(id) = self.selected else {
             self.error = Some("Create or select a task first.".into());
             cx.notify();
diff --git a/crates/synara-app/src/shell/goals.rs b/crates/synara-app/src/shell/goals.rs
index c927092..6c58980 100644
--- a/crates/synara-app/src/shell/goals.rs
+++ b/crates/synara-app/src/shell/goals.rs
@@ -183,6 +183,15 @@ impl Shell {
             cx,
         );
     }
+    pub(super) fn goal_send_blocked(&mut self, cx: &mut Context<Self>) -> bool {
+        if self.goals.busy || self.goals.deferred.is_some() || self.goals.dirty(cx) {
+            self.notice = Some("Save or discard goal edits and let the pending save finish before sending.".into());
+            cx.notify();
+            true
+        } else {
+            false
+        }
+    }
     pub(super) fn goal_navigation_blocked(&mut self, cx: &mut Context<Self>) -> bool {
         if self.goals.lease.is_some() {
             self.pause_goals(
@@ -786,7 +795,7 @@ impl Shell {
         );
         if let Some(v) = &g.value {
             let elapsed = v.elapsed_ms
-                + g.lease.as_ref().map_or(0, |l| {
+                + g.lease.as_ref().filter(|l| l.begun).map_or(0, |l| {
                     l.accounted
                         .elapsed()
                         .as_millis()
diff --git a/crates/synara-app/src/shell/hubs.rs b/crates/synara-app/src/shell/hubs.rs
index 57b05b4..1aae705 100644
--- a/crates/synara-app/src/shell/hubs.rs
+++ b/crates/synara-app/src/shell/hubs.rs
@@ -75,6 +75,15 @@ impl Shell {
     }
     pub(super) fn hub_navigation_blocked(&mut self, cx: &mut Context<Self>) -> bool {
         if self.revision_navigation_blocked(cx) { return true; }
+        self.hub_editors_blocked(cx)
+    }
+    // Sending in place is not navigation. Keep the reviewed goal lease, but retain
+    // every unsaved-editor guard used by navigation.
+    pub(super) fn hub_send_blocked(&mut self, cx: &mut Context<Self>) -> bool {
+        if self.goal_send_blocked(cx) || self.revision_navigation_except_goal(cx) { return true; }
+        self.hub_editors_blocked(cx)
+    }
+    fn hub_editors_blocked(&mut self, cx: &mut Context<Self>) -> bool {
         if self.followup_navigation_blocked(cx) { return true; }
         if !self.hubs.pending(cx) { return false; }
         self.error = Some("Save or explicitly discard the Hub editor before leaving it.".into());
diff --git a/scripts/native_goals_smoke.py b/scripts/native_goals_smoke.py
index a1b6c8b..4525ed8 100644
--- a/scripts/native_goals_smoke.py
+++ b/scripts/native_goals_smoke.py
@@ -38,6 +38,7 @@ def run(s):
     click(s,'goal-open')
     fill(s,'composer-input','')
     click(s,'goal-resume');click(s,'composer-submit')
+    wait_until(lambda: count(s)>initial,'explicit Send keeps the goal lease and starts the turn')
     wait_until(lambda: preference(s,key)['status']=='blocked','bounded goal pursuit exhausted',30)
     assert 'budget' in preference(s,key)['note'] and count(s)==initial+3
     assert preference(s,key)['elapsed_ms']>0
@@ -86,7 +87,15 @@ def run(s):
     s.desktop.screenshot('goal-achievement-review',window_only=True)
     close(s);s.launch(preserve_selection=True)
     assert preference(s,key)==saved
-    click(s,'goal-open');click(s,'goal-clear')
+    click(s,'goal-open')
+    save(s,task,'fixture-goal-budget')
+    initial=count(s)
+    click(s,'goal-resume')
+    click(s,'new-thread')
+    wait_until(lambda:'Navigation paused' in preference(s,key)['note'],'navigation disarms the goal')
+    assert count(s)==initial and selection(s)==task
+    s.checks.append('Send-preserves-reviewed-lease-but-navigation-disarms-without-launch')
+    click(s,'goal-clear')
     wait_until(lambda:not preference(s,key)['objective'],'explicit clear')
     assert preference(s,key)['achievements']==[]
     click(s,'goal-close');click(s,'new-thread')
