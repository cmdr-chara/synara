diff --git a/crates/synara-app/src/shell/debug_workflow.rs b/crates/synara-app/src/shell/debug_workflow.rs
index 44fa21e..e0c79f1 100644
--- a/crates/synara-app/src/shell/debug_workflow.rs
+++ b/crates/synara-app/src/shell/debug_workflow.rs
@@ -72,7 +72,7 @@ impl Shell {
         });
     }
     pub(super) fn open_debug(&mut self, cx: &mut Context<Self>) {
-        if self.selected.is_none() {
+        if self.selected.is_none() || self.loading_task.is_some() || self.debug_workflow.busy {
             return;
         }
         self.show_conversation(cx);
@@ -198,7 +198,9 @@ impl Shell {
                 view.open,
                 cx.listener(|this, _: &(), _, cx| this.open_debug(cx)),
             )
-            .child(ui::layout_probe("debug-open")),
+            .child(ui::layout_probe_enabled(
+                "debug-open", !view.busy && self.loading_task.is_none(),
+            )),
         );
         if !view.open {
             return root.into_any_element();
diff --git a/crates/synara-app/src/shell/recap.rs b/crates/synara-app/src/shell/recap.rs
index e302d6f..66f92dd 100644
--- a/crates/synara-app/src/shell/recap.rs
+++ b/crates/synara-app/src/shell/recap.rs
@@ -7,6 +7,7 @@ pub(super) struct RecapState {
     epoch: u64,
     open: bool,
     busy: bool,
+    loading: bool,
     creating: bool,
     cached: Option<ThreadRecap>,
     origin: Option<ThreadOrigin>,
@@ -42,6 +43,7 @@ impl RecapState {
             epoch: 0,
             open: false,
             busy: false,
+            loading: false,
             creating: false,
             cached: None,
             origin: None,
@@ -52,7 +54,8 @@ impl RecapState {
         }
     }
     pub fn pending(&self) -> bool {
-        self.review.is_some() || self.busy
+        // Cache/origin reads carry a task/epoch fence and own no unsaved work.
+        self.review.is_some() || self.busy && !self.loading
     }
 }
 impl Shell {
@@ -69,6 +72,7 @@ impl Shell {
         self.recap.epoch = self.recap.epoch.wrapping_add(1);
         self.recap.open = false;
         self.recap.busy = true;
+        self.recap.loading = true;
         self.recap.cached = None;
         self.recap.origin = None;
         self.recap.review = None;
@@ -101,7 +105,7 @@ impl Shell {
         let Some(task) = self.selected.filter(|id| Some(*id) == self.recap.task) else {
             return;
         };
-        if self.recap.pending()
+        if self.recap.busy || self.recap.pending()
             || self.busy.contains(&task)
             || self.connecting.contains(&task)
             || self.loading_task.is_some()
@@ -163,7 +167,7 @@ impl Shell {
         let Some(task) = self.selected.filter(|id| Some(*id) == self.recap.task) else {
             return;
         };
-        if self.recap.pending()
+        if self.recap.busy || self.recap.pending()
             || self.busy.contains(&task)
             || self.loading_task.is_some()
             || self.close != CloseState::Open
@@ -194,6 +198,7 @@ impl Shell {
             return;
         }
         self.recap.busy = false;
+        self.recap.loading = false;
         if self.recap.creating {
             self.creating_task = false;
             self.recap.creating = false;
diff --git a/scripts/native_debug_smoke.py b/scripts/native_debug_smoke.py
index 80e045f..4ced548 100644
--- a/scripts/native_debug_smoke.py
+++ b/scripts/native_debug_smoke.py
@@ -16,7 +16,7 @@ def run(s):
     task = selection(s)
     original = task_events(s, task)
     key = 'task-debug:' + task
-    click(s, 'debug-open')
+    s.click_control('debug-open', enabled=True)
     click(s, 'debug-enabled')
     wait_until(lambda: (preference(s, key) or {}).get('enabled'), 'Debug enabled without agent start')
     assert task_events(s, task) == original
@@ -56,7 +56,7 @@ def run(s):
     assert preference(s, key) == saved and task_events(s, task) == original
     s.click_control('composer-input')
     assert s.desktop.copy_input() == draft
-    click(s, 'debug-open')
+    s.click_control('debug-open', enabled=True)
     s.desktop.screenshot('debug-restart-paused', window_only=True)
     s.checks.append('mode-evidence-completion-and-draft-survive-restart-without-execution')
     click(s, 'debug-close')
